* Add PlayerState enum to Player
	- Standing
	- Moving
	- Attacking
		=> When Standing, can Move, Attack, Talk
		=> When Moving, can Attack, Stand, Talk
		=> When Attacking, must wait the end before doing something else

* Add States objects
	=> MapState
	=> DialogState
	=> TitleScreenState
	=> MenuState

